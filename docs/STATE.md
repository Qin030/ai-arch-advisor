# 現在狀態

<!-- 只記錄機器查不到的狀態。PR、issue、CI 的即時狀況由 /start 查 GitHub，
     不要寫進這裡——寫了會過時。本檔變更走 PR，內容穩定所以不痛。 -->

**日期：D5（2026-09-07）。** 日號改以日曆推進，錨定最後一天 **D10 = 2026-09-12**
（M1 於 2026-09-07 決定；先前以里程碑推進，兩種算法差三天以上）。對照表：

| D1 | D2 | D3 | D4 | D5 | D6 | D7 | D8 | D9 | D10 |
|---|---|---|---|---|---|---|---|---|---|
| 09-03 | 09-04 | 09-05 | 09-06 | **09-07** | 09-08 | 09-09 | **09-10** | 09-11 | **09-12** |

D1 的實際工期是 08-28～09-02（六個日曆天），契約於 09-02 凍結；上表是**往回錨定
交件日**的結果，不是歷史工期。

**階段：** 開發中。**功能凍結是 2026-09-10（D8）**，之後只收 `fix:` 與 `docs:`。

**進度落差（機器查不到的判斷，非 GitHub 狀態）：**
- **D2「走通骨架」尚未達成**——`make demo` 綠，但實際呼叫三個 endpoint 全數回 501
- **D4 未開工**——知識庫 0／25 筆，檢索層是空的
- **D5「真實端到端」是今天到期，未達成**——規則層是空的
- 依 `docs/PLAN.md` 第八節，「檢索層做不完（D5 收工未綠）→ 砍掉向量檢索，改純
  關鍵字 ＋ metadata 過濾」的停損條件**已經成立**，待 M1 決定是否啟動
**契約：** **已凍結**（2026-09-02，M2 複審通過，PR #4；issue #1 已關閉）

**已知未補（刻意延後）：**
- 規格與測試的涵蓋率沒有機器檢查 → D8 題組驗證時人工對照一次
- STATE.md 靠人工更新，D1 已證實會落後（M2 8/29 交出審查、M1 停滯 4 天無人察覺）→ 每天開工先跑 /start
- PR 400 行上限、agent 檔案重疊偵測 → 刻意不做機器檢查，留給人判斷
- `make demo` 只跑 `tests/smoke`，不是端到端情境。`/eod` 第 4 步要驗的「實際跑
  一次臺南兩層透天」它驗不到——2026-09-07 實測 `make demo` 綠燈但 `/session/start`
  回 501。**綠燈不等於 demo 跑得動**，收工時要另外實際呼叫一次
- `make index` 與 `make eval` 指向 `scripts/build_index.py`、`scripts/run_eval.py`，
  兩個檔案都還不存在（分別是 D4 與 D8 的工作）。`make eval` 的產出
  `docs/eval-report.md` 是企畫書表 3 的資料來源 → D8 當天才發現腳本不存在會來不及
- `app/rules/CLAUDE.md` 情境 1 範圍可能不足（**待人工查證，非確定結論**）：
  臺南市的都市計畫容積率／建蔽率規定，可能依各別都市計畫書另定，未必
  只靠地號與使用分區就能判定確切數值。但實際依據的條文、條號、百分比
  是否適用於臺南市——`TODO: 待人工查證，需開啟都市計畫法臺南市施行
  細則的官方原文並記錄條號、內容、修正日期`。查證屬實後 D5 再評估是否
  拆成獨立拒答，`confirm_with` 的正確窗口名稱同樣待查證。
  （前一版曾寫入具體條號、百分比與 Claude Code 用 WebFetch 查核的結果
  ——M2 複審判定 agent 查網頁不能取代規則要求的人工核對，已依她的意見
  全部移除，不留具體數字。）
- KB 版本日期陷阱：法規資料庫的頁首日期可能只反映當次修正動到的少數
  條文，不代表全部條文都是那個日期。實際受影響的條號與確切日期跨度
  ——`TODO: 待人工查證`。D4 建立 `VERIFIED.md` 台帳時，每筆都要逐筆
  核對該條自己的沿革頁，不能只看頁首日期。
