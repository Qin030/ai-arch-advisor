# AGENTS.md — AI 建築前期決策助理

## 專案摘要

把一般人「想要溫馨的家」這類感受語彙轉譯為可設計條件，產出建築需求摘要、家庭數位生活需求摘要、待專業人員確認清單三份文件。技術堆疊：Python / FastAPI / LangChain / ChromaDB / Streamlit。

**核心差異化是「該拒答時拒答」。** 資料不足時系統應該說「無法判定，請補充 X」，而不是給一個聽起來合理的答案。

## 先確認你在哪個角色

你可能被 M1 或 M2 呼叫，兩者任務完全不同：

| 呼叫者 | 你的角色 | 你能碰的目錄 |
|---|---|---|
| **M2** | 主力實作者 | `data/kb/`、`ui/`、`tests/spec/`、`scripts/` |
| **M1** | 輔助（寫 script、文件、素材處理） | 僅限當次明確指定的檔案 |

不確定是誰在呼叫你 → 問。

## ⚠️ 寫測試時的絕對禁令

當你的任務是**依規格撰寫測試**（`tests/spec/` 下的檔案）：

**不得閱讀 `app/rules/`、`app/core/`、`app/retrieval/` 的任何實作程式碼。**

只能讀 `docs/CONTRACT.md`、`schema/`、issue 描述。

原因：你一旦看過實作，寫出來的測試就會迎合實作的行為，包括迎合它的 bug。這些測試是 M1 的 Claude Code 要照著寫實作的契約，契約必須獨立於實作產生，否則整個「產出者 ≠ 審查者」的機制就是空的。

如果規格不足以寫出測試 → **停下來列出缺什麼**，不要去看實作補齊。

## 鐵律

> 以下內容複製自 `docs/agents/RULES.md`。**修改時兩處都要改**，PR 模板有對應 checkbox。

1. **產出者 ≠ 審查者。** 寫實作的 agent 不得寫該功能的測試；寫測試的 agent 在測試合併前不得讀實作；開 PR 的人不得 approve 自己的 PR。
2. **Agent 不得合併。** 可以 commit、開 PR、留 comment，但 merge 一律由人類執行，且必須是另一位成員。
3. **不得捏造來源。** 法規條號、條文、統計數字、版本日期只能來自人工提供的原始資料。找不到就寫 `TODO: 待人工查證`。**這是唯一一條違反即停工的規則。**
4. **拒答是功能，不是失敗。** 不得為了讓回答完整而推定、補值或引用鄰近地區規定。
5. **單一 PR diff 上限 400 行。** 超過就停下來回報怎麼拆。
6. **不得越界修改。** 只改與當前 issue 相關的檔案，不順手重構。
7. **功能凍結後（D8 起）** 只接受 `fix:` 與 `docs:`。
8. **語言：** 程式碼與 commit 用英文，使用者可見輸出與文件用繁體中文（臺灣用語）。
9. **不確定就停。** 規格不明、契約沒寫、要動別人的目錄 → 問人。

## 你負責的區塊

- `data/kb/` — 知識庫切片。**進去前先讀 `data/kb/AGENTS.md`，那裡有本專案最嚴格的限制。**
- `ui/` — Streamlit 三畫面（智慧追問 / 多方案比較 / 來源標註展開）
- `tests/spec/` — 規格測試（見上方禁令）
- `scripts/` — 驗證與工具腳本

## 不要建空 stub

還沒排到的檔案就是不存在，不要先建空殼佔位（例如 D2 才要動 `ui/views/compare.py`，D1 別先生一個空檔案）。空檔案會誘使之後的 session 順手補完，跳過它本該對齊的測試，也讓人分不出「還沒開始」和「開始了但壞掉」。當天真正該產出的里程碑檔案不算 stub，要寫完整內容——例如 D1 的 `schema/requirement.schema.json` 就是 Day 1 的正式產出，不是先佔位。

## 指令

```bash
make demo          # 起 API + UI
make test          # pytest
make lint          # ruff
python scripts/validate_kb.py
```

## UI 的特別要求

D9 要拿 UI 截圖放進企畫書的圖 3，所以三個畫面必須好看且可截圖：

1. **智慧追問** — 勾選式選項 ＋ 每題下方的「為什麼問這題」
2. **多方案比較** — 並列表格，欄位：構造與外牆策略／工程費區間／夏季熱環境（相對）／待確認事項
3. **來源標註展開** — 可摺疊，展開後顯示發布機關、版本日期、適用地區、連結

不要用 emoji 當圖示，不要用未經設計的預設配色。

## 規劃文件只有 RULES.md 用 @ 匯入

`docs/PLAN.md`、`docs/REPO_STRUCTURE.md`、`docs/agents/SETUP.md`、`docs/agents/ROSTER.md` 這幾份都很長、多數內容用不到，**不要**改成 `@` 匯入——每個 session 開場都會白燒 context。新增規劃類文件時比照辦理，列進下方表格就好，不要加 `@`。目前唯一的例外是 `docs/agents/RULES.md`，因為那是每次都要遵守的鐵律，不是查閱資料。

## 開工程序（每次對話開始時執行）

你沒有 slash command，所以這幾步靠你自己做。**做完之前不要寫任何程式碼。**

1. 讀 `docs/STATE.md` — 現在第幾天、契約凍結了沒、有什麼卡住
2. 讀本檔的「當前階段」那一行
3. 回報你理解的狀態，以及你認為今天該做什麼
4. **等人類確認之後**才開始

### 四個必須先確認的事

- **契約凍結了嗎？** `docs/STATE.md` 寫「未凍結」時，不要實作任何依賴 schema 欄位的東西
- **現在是第幾天？** D8 起只接受 `fix:` 與 `docs:`，收到新功能請求就拒絕
- **這件事有 issue 嗎？有驗收標準嗎？** 沒有就先問，不要自己定義完成條件
- **這是我的目錄嗎？** 你只能碰 `data/kb/`、`ui/`、`tests/spec/`、`scripts/`

### 寫測試時的額外檢查

任務是寫 `tests/spec/` 的測試 → 先確認對應的 `docs/specs/*.md` 已經合併進 main。
規格還沒進 main 就不能開始寫，因為你會忍不住去看實作。

## 需要時才讀的文件

以下不會自動載入（太長，會排擠 context）。需要時再自己開：

| 檔案 | 什麼時候該讀 |
|---|---|
| `docs/CONTRACT.md` | 動到 API 或資料結構前 |
| `docs/SCOPE.md` | 有人要求新功能時，先確認在不在清單上 |
| `docs/REPO_STRUCTURE.md` | 不確定一個新檔案該放哪 |
| `docs/PLAN.md` | 需要知道某項工作排在第幾天、誰負責 |
| `docs/agents/ROSTER.md` | 需要跨 agent 交接 |

## 當前階段

**D1–D7：開發中**

<!-- 階段變更時由 M1 手動改這一行 -->
